#pragma config(I2C_Usage, I2C1, i2cSensors)
#pragma config(Sensor, in1,    otherBattery,   sensorAnalog)
#pragma config(Sensor, in8,    armAngle,       sensorPotentiometer)
#pragma config(Sensor, dgtl12, skyriseClaw,    sensorDigitalOut)
#pragma config(Sensor, I2C_1,  ,               sensorQuadEncoderOnI2CPort,    , AutoAssign)
#pragma config(Sensor, I2C_2,  ,               sensorQuadEncoderOnI2CPort,    , AutoAssign)
#pragma config(Motor,  port1,           cubeIntake,    tmotorVex393_HBridge, openLoop, reversed)
#pragma config(Motor,  port2,           lmDrive,       tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port3,           lbDrive,       tmotorVex393_MC29, openLoop, encoderPort, None)
#pragma config(Motor,  port4,           lfLift,        tmotorVex393_MC29, openLoop, reversed)
#pragma config(Motor,  port5,           rDrive,        tmotorVex393_MC29, openLoop, reversed)
#pragma config(Motor,  port6,           skyriseArm,    tmotorVex393_MC29, openLoop, reversed, encoderPort, None)
#pragma config(Motor,  port7,           rfLift,        tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port8,           rbLift,        tmotorVex393_MC29, openLoop, encoderPort, I2C_2)
#pragma config(Motor,  port9,           lbLift,        tmotorVex393_MC29, openLoop, reversed, encoderPort, I2C_1)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

#pragma platform(VEX)
//Competition Control and Duration Settings
#pragma competitionControl(Competition)
#pragma autonomousDuration(15)
#pragma userControlDuration(105)
#include "Vex_Competition_Includes.h"
#include "enumerations.h"
#include "autonomous.h"
#include "lcd.h"
bool onePoint = false;
void pre_auton(){
	bLCDBacklight = true;
	clearLCD();
	displayLCDCenteredString(0, "Autonomous?");
	displayLCDCenteredString(1, "Red    1    Blue");
	while(true){
		if(nLCDButtons == MHLCDButtonLeft){
			initSkyriseIntakeWithTeamColor(MHTeamColorRed);
			break;
		}
		else if(nLCDButtons == MHLCDButtonRight){
			initSkyriseIntakeWithTeamColor(MHTeamColorBlue);
			break;
		}
		else if(nLCDButtons == MHLCDButtonCenter){
			onePoint = true;
			initSkyriseIntakeWithTeamColor(MHTeamColorAny);
			break;
		}
	}
	clearLCD();
	bLCDBacklight = false;
}
task autonomous(){
	if(onePoint){
		basicDrive(MHMotorPowerMax, MHMotorPowerMax);
		wait1Msec(MHTimeOneSecond);
		stopDrive();
		//Auton over
		return;
	}
	else{
		liftCubeForTime(MHTimeQuarterSecond, MHLiftDirectionDown);
		for(int i = 1; i <=5; i++){
			placeSkyrise(MHSkyriseForInt(i));
		}
	}
	//switch(autonSelection){
	//	case MHTeamColorBlue:
	//		liftForEncoderDistance(2000, MHMotorPowerMax);
	//		basicDrive(MHMotorPowerMax, MHMotorPowerStop);
	//		wait1Msec(MHTimeOneSecond);
	//		basicDrive(MHMotorPowerStop, -MHMotorPowerMax);
	//		wait1Msec(MHTimeHalfSecond);
	//		basicDrive(MHMotorPowerMax, MHMotorPowerMax);
	//		wait1Msec(MHTimeTenthSecond);
	//		stopDrive();
	//		liftForEncoderDistance(1500, -MHMotorPowerMax);
	//		liftCube(MHMotorPowerMax, MHLiftDirectionDown);
	//		wait1Msec(MHTimeOneSecond + MHTimeHalfSecond);
	//		basicDrive(-MHMotorPowerMax, MHMotorPowerStop);
	//		stopDrive();
	//		wait1Msec(MHTimeOneSecond);
	//		//Auton over
	//		return;
	//	case MHTeamColorRed:
	//		liftForEncoderDistance(2000, MHMotorPowerMax);
	//		basicDrive(MHMotorPowerStop, MHMotorPowerMax);
	//		wait1Msec(MHTimeOneSecond);
	//		basicDrive(-MHMotorPowerMax, MHMotorPowerStop);
	//		wait1Msec(MHTimeHalfSecond);
	//		basicDrive(MHMotorPowerMax, MHMotorPowerMax);
	//		wait1Msec(MHTimeTenthSecond);
	//		stopDrive();
	//		liftForEncoderDistance(1500, -MHMotorPowerMax);
	//		liftCube(MHMotorPowerMax, MHLiftDirectionDown);
	//		wait1Msec(MHTimeOneSecond + MHTimeHalfSecond);
	//		basicDrive(MHMotorPowerStop, -MHMotorPowerMax);
	//		wait1Msec(MHTimeOneSecond);
	//		stopDrive();
	//		//Auton over
	//		return;
	//	default:
	//		basicDrive(MHMotorPowerMax, MHMotorPowerMax);
	//		wait1Msec(MHTimeOneSecond);
	//		stopDrive();
	//		//Auton over
	//		return;

	//		//Programming Skills, for later (scores 7)

	//		//liftForEncoderDistance(2000, MHMotorPowerMax);
	//		//basicDrive(MHMotorPowerStop, MHMotorPowerMax);
	//		//wait1Msec(MHTimeOneSecond);
	//		//basicDrive(-MHMotorPowerMax, MHMotorPowerStop);
	//		//wait1Msec(MHTimeHalfSecond);
	//		//basicDrive(MHMotorPowerMax, MHMotorPowerMax);
	//		//wait1Msec(MHTimeTenthSecond);
	//		//basicDrive(MHMotorPowerStop, MHMotorPowerStop);
	//		//liftForEncoderDistance(1500, -MHMotorPowerMax);
	//		//liftCube(MHMotorPowerMax, MHLiftDirectionDown);
	//		//wait1Msec(MHTimeOneSecond + MHTimeHalfSecond);
	//		//basicDrive(MHMotorPowerStop, -MHMotorPowerMax);
	//		//wait1Msec(MHTimeOneSecond);
	//		//basicDrive(-MHMotorPowerMax, -MHMotorPowerMax);
	//		//wait1Msec(MHTimeOneSecond);
	//		//basicDrive(MHMotorPowerMax, MHMotorPowerStop);
	//		//wait1Msec(MHTimeQuarterSecond);
	//		//basicDrive(MHMotorPowerMax, MHMotorPowerMax);
	//		//wait1Msec(MHTimeOneSecond * 2);
	//		//basicDrive(-MHMotorPowerMax, -MHMotorPowerMax);
	//		//wait1Msec(MHTimeHalfSecond);
	//		//stopDrive();
	//		//Programming Skills over
	//		//return;
	//	}
}
task usercontrol(){
	//Drive control
	bLCDBacklight = true;
	string first;
	string second;
	while(true){
		if(nMotorEncoder[lbLift] < 0){
			resetEncoders();
		}
		if(nLCDButtons != MHLCDButtonNone){
			resetEncoders();
		}
		sprintf(first, "%d", nMotorEncoder[lbLift]);
		sprintf(second, "%d", SensorValue[armAngle]);
		displayLCDCenteredString(0, first);
		displayLCDCenteredString(1, second);
		if(abs(vexRT[Ch2]) <= 30){
			stopDriveSide(MHRobotSideRight);
		}
		else{
			motor[rDrive] = vexRT[Ch2];
		}
		if(abs(vexRT[Ch3]) <= 30){
			stopDriveSide(MHRobotSideLeft);
		}
		else{
			motor[lmDrive] = motor[lbDrive] = vexRT[Ch3];
		}
		//Lift control
		if(vexRT[Btn6U]){
			lift(MHMotorPowerMax, MHLiftDirectionUp);
		}
		else if(vexRT[Btn6D]){
			lift(MHMotorPowerMax, MHLiftDirectionDown);
		}
		//If neither button is pressed, the lift must be stopped, or it will keep going
		else{
			lift(MHMotorPowerStop, MHLiftDirectionStop);
		}
		//Skyrise Intake
		if(vexRT[Btn8D]){
			SensorValue[skyriseClaw] = MHPneumaticPositionOpen;
		}
		else if(vexRT[Btn8R]){
			SensorValue[skyriseClaw] = MHPneumaticPositionClosed;
		}
		//Skyrise arm rotation
		if(vexRT[Btn7L]){
			motor[skyriseArm] = MHMotorPowerMax;
		}
		else if(vexRT[Btn7R]){
			motor[skyriseArm] = -MHMotorPowerMax;
		}
		else{
			motor[skyriseArm] = MHMotorPowerStop;
		}
		//Cube Intake
		if(vexRT[Btn5U]){
			liftCube(MHLiftDirectionUp);
		}
		else if(vexRT[Btn5D]){
			liftCube(MHLiftDirectionDown);
		}
		else{
			liftCube(MHLiftDirectionStop);
		}
	}
}
